apiVersion: v1
kind: Service
metadata:
  name: {{ include "m537-voice-gateway.fullname" . }}
  labels:
    {{- include "m537-voice-gateway.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "m537-voice-gateway.selectorLabels" . | nindent 4 }}
